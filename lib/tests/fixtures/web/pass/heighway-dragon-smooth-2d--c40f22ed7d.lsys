# ID       : LSYS-C40F22ED7DAF
# Name     : Heighway Dragon Smooth (2D)
# Source   : gerizim16/3d-l-system-generator preset catalog | preset 12: Heighway Dragon Smooth (2D) | https://github.com/gerizim16/3d-l-system-generator/blob/master/src/utils/presets.js
# Formalism: parametric stochastic context-free 3-D scene grammar
# Parse    : pass; validator=corpus-static
# Execute  : not-run; target=attached-cpu
# Render   : unsupported; reason=unsupported render profile 'turtle3d-gerizim'
# Fidelity : projection
# Features : context-free, deterministic, parametric, scene-primitives, turtle3d
# Angle    : 90
# Heading  : 0
# Draw     : f l cone sphere cube
# Move     : none
# Profile  : turtle3d-gerizim
# Fingerprint: sha256:c40f22ed7daf804ac201279a880b94be5f218b13d75d20f0b1d0d8363d94dac7
# Note     : random() source expressions are projected to the deterministic global RANDOM=0.5 because the target expression language has no RNG builtin.
#
# Original gerizim16/3d-l-system-generator preset catalog:
# GERIZIM | name: "Heighway Dragon Smooth (2D)",
# GERIZIM |     lsystem: {
# GERIZIM |       axiom: "s t{0.7} f X e",
# GERIZIM |       productions:
# GERIZIM |         "X -> X +x Y f +x\nY -> -x f X -x Y",
# GERIZIM |       iterations: 5,
# GERIZIM |     },
# GERIZIM |     defaults: { length: 0.4, angle: 90
#
let PI = 3.14159265359;
let RANDOM = 0.5;
let ANGLE = 90;

axiom s t(0.7) f X e;

match X then X PitchUp Y f PitchUp;
match Y then PitchDown f X PitchDown Y;
