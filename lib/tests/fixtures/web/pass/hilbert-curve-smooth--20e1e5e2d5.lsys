# ID       : LSYS-20E1E5E2D5F5
# Name     : Hilbert curve smooth
# Source   : gerizim16/3d-l-system-generator preset catalog | preset 4: Hilbert curve smooth | https://github.com/gerizim16/3d-l-system-generator/blob/master/src/utils/presets.js
# Formalism: parametric stochastic context-free 3-D scene grammar
# Parse    : pass; validator=corpus-static
# Execute  : not-run; target=attached-cpu
# Render   : unsupported; reason=unsupported render profile 'turtle3d-gerizim'
# Fidelity : projection
# Features : context-free, deterministic, parametric, scene-primitives, turtle3d
# Angle    : 90
# Heading  : 0
# Draw     : f l cone sphere cube
# Move     : none
# Profile  : turtle3d-gerizim
# Fingerprint: sha256:20e1e5e2d5f571252c43faef66223dab588b9dcfbe30ed3de2ec352aa24045d1
# Note     : random() source expressions are projected to the deterministic global RANDOM=0.5 because the target expression language has no RNG builtin.
#
# Original gerizim16/3d-l-system-generator preset catalog:
# GERIZIM | name: "Hilbert curve smooth",
# GERIZIM |     lsystem: {
# GERIZIM |       axiom: "t{0.5} s X e",
# GERIZIM |       productions:
# GERIZIM |         "X -> +x -z X f +x -z X f X +y f +x +z +z X f X -x f -y +z +z X f X +y f +z X +y +z",
# GERIZIM |       iterations: 3,
# GERIZIM |     },
# GERIZIM |     defaults: { length: 1, angle: 90
#
let PI = 3.14159265359;
let RANDOM = 0.5;
let ANGLE = 90;

axiom t(0.5) s X e;

match X then PitchUp Minus X f PitchUp Minus X f X RollLeft f PitchUp Plus Plus X f X PitchDown f RollRight Plus Plus X f X RollLeft f Plus X RollLeft Plus;
