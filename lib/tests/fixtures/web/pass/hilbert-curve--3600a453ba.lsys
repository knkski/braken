# ID       : LSYS-3600A453BAFA
# Name     : Hilbert curve
# Source   : gerizim16/3d-l-system-generator preset catalog | preset 3: Hilbert curve | https://github.com/gerizim16/3d-l-system-generator/blob/master/src/utils/presets.js
# Formalism: parametric stochastic context-free 3-D scene grammar
# Parse    : pass; validator=corpus-static
# Execute  : not-run; target=attached-cpu
# Render   : unsupported; reason=unsupported render profile 'turtle3d-gerizim'
# Fidelity : projection
# Features : context-free, deterministic, parametric, scene-primitives, turtle3d
# Angle    : 90
# Heading  : 0
# Draw     : f l cone sphere cube
# Move     : none
# Profile  : turtle3d-gerizim
# Fingerprint: sha256:3600a453bafa535c2adad0ad7263714474b5ea121faebcbbf724178c3f7b5219
# Note     : random() source expressions are projected to the deterministic global RANDOM=0.5 because the target expression language has no RNG builtin.
#
# Original gerizim16/3d-l-system-generator preset catalog:
# GERIZIM | name: "Hilbert curve",
# GERIZIM |     lsystem: {
# GERIZIM |       axiom: "X",
# GERIZIM |       productions:
# GERIZIM |         "X -> +x -z X l +x -z X l X +y l +x +z +z X l X -x l -y +z +z X l X +y l +z X +y +z",
# GERIZIM |       iterations: 3,
# GERIZIM |     },
# GERIZIM |     defaults: { length: 1, angle: 90
#
let PI = 3.14159265359;
let RANDOM = 0.5;
let ANGLE = 90;

axiom X;

match X then PitchUp Minus X l PitchUp Minus X l X RollLeft l PitchUp Plus Plus X l X PitchDown l RollRight Plus Plus X l X RollLeft l Plus X RollLeft Plus;
