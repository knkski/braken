# ID       : LSYS-ABF0699E5C69
# Name     : Pattern (2D)
# Source   : gerizim16/3d-l-system-generator preset catalog | preset 14: Pattern (2D) | https://github.com/gerizim16/3d-l-system-generator/blob/master/src/utils/presets.js
# Formalism: parametric stochastic context-free 3-D scene grammar
# Parse    : pass; validator=corpus-static
# Execute  : not-run; target=attached-cpu
# Render   : unsupported; reason=unsupported render profile 'turtle3d-gerizim'
# Fidelity : projection
# Features : context-free, deterministic, parametric, scene-primitives, turtle3d
# Angle    : 90
# Heading  : 0
# Draw     : f l cone sphere cube
# Move     : none
# Profile  : turtle3d-gerizim
# Fingerprint: sha256:abf0699e5c69ff3232ef6cdc2a4d6510ca3578d72dd325cf1671e3f6e3db641d
# Note     : random() source expressions are projected to the deterministic global RANDOM=0.5 because the target expression language has no RNG builtin.
#
# Original gerizim16/3d-l-system-generator preset catalog:
# GERIZIM | name: "Pattern (2D)",
# GERIZIM |     lsystem: {
# GERIZIM |       axiom: "s f e",
# GERIZIM |       productions:
# GERIZIM |         "f -> f +x f -x f -x f +x f",
# GERIZIM |       iterations: 4,
# GERIZIM |     },
# GERIZIM |     defaults: { length: 0.1, angle: 90
#
let PI = 3.14159265359;
let RANDOM = 0.5;
let ANGLE = 90;

axiom s f e;

match f then f PitchUp f PitchDown f PitchDown f PitchUp f;
